package com.example.hospitalfinder
import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

fun automaticallyLocateHospitals()
{

}

fun navigateToProductDetails(productId: String)
{
    //val directions = ProductListDirection.navigateToProductDetail(productId)
}

class ProductListDirection {
    //var navigateToProductDetails
}

class ProductListActivity: AppCompatActivity() {
    // Views and/or ViewDataBinding references, Adapters
    private lateinit var productAdapter:ProductAdapter

    // fun onCreate(savedInstanceState: Bundle){
       //  super.onCreate(savedInstanceState)
         /* Replace setContentView(R.layout.product_list) with the line */
   // }
}

class ProductAdapter {

}
